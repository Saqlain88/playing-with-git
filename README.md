# FastAPI GitHub Actions Demo

A simple FastAPI application used to learn GitHub Actions CI/CD.

## Run locally

Create a virtual environment:

```bash
python -m venv .venv
```

To run tests:

```bash
python.exe -m pytest -q
```
